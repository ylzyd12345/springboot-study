# Junmo Platform 开发指南

## 📋 文档信息

| 项目 | 内容 |
|------|------|
| **文档名称** | Junmo Platform 开发指南 |
| **版本号** | v1.0.0 |
| **创建日期** | 2025-12-24 |
| **作者** | 技术架构师 |
| **审核人** | 开发团队负责人 |
| **批准人** | 技术总监 |

## 🎯 开发环境搭建

### 系统要求

#### 硬件要求
- **CPU**: Intel i5 或 AMD Ryzen 5 以上
- **内存**: 最少8GB，推荐16GB+
- **存储**: 至少50GB可用空间
- **网络**: 稳定的互联网连接

#### 软件要求

| 软件 | 版本要求 | 说明 |
|------|----------|------|
| 操作系统 | Windows 10+, macOS 10.15+, Ubuntu 18.04+ | 支持主流操作系统 |
| JDK | OpenJDK 25+ | Java开发环境 |
| Maven | 3.9.0+ | 项目构建工具 |
| Git | 2.30+ | 版本控制工具 |
| IDE | IntelliJ IDEA 2023+ / Eclipse 2023+ | 开发IDE |
| Docker | 20.0+ | 容器运行环境 |
| Node.js | 18+ | 前端开发环境（可选） |
| Sa-Token | 1.37+ | 轻量级权限认证框架 |
| MyBatis-Plus | 3.5+ | 数据访问层增强框架 |

### 环境配置

#### 1. JDK安装配置

**Windows环境**:
```bash
# 下载并安装OpenJDK 25
# 设置环境变量
JAVA_HOME=C:\Program Files\Java\jdk-25
PATH=%JAVA_HOME%\bin;%PATH%

# 验证安装
java -version
javac -version
```

**macOS环境**:
```bash
# 使用Homebrew安装
brew install openjdk@25

# 设置环境变量
echo 'export JAVA_HOME=/opt/homebrew/opt/openjdk@25' >> ~/.zshrc
echo 'export PATH="$JAVA_HOME/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc

# 验证安装
java -version
```

**Linux环境**:
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install openjdk-25-jdk

# 设置环境变量
echo 'export JAVA_HOME=/usr/lib/jvm/java-25-openjdk-amd64' >> ~/.bashrc
echo 'export PATH="$JAVA_HOME/bin:$PATH"' >> ~/.bashrc
source ~/.bashrc

# 验证安装
java -version
```

#### 2. Maven配置

**下载Maven**:
```bash
# 下载Maven 3.9.12
wget https://dlcdn.apache.org/maven/maven-3/3.9.12/binaries/apache-maven-3.9.12-bin.tar.gz

# 解压安装
tar -xzf apache-maven-3.9.12-bin.tar.gz
sudo mv apache-maven-3.9.12 /opt/maven
```

**配置环境变量**:
```bash
# ~/.bashrc 或 ~/.zshrc
export MAVEN_HOME=/opt/maven
export PATH=$MAVEN_HOME/bin:$PATH
```

**配置Maven镜像**:
```xml
<!-- ~/.m2/settings.xml -->
<settings>
  <mirrors>
    <mirror>
      <id>aliyun-maven</id>
      <mirrorOf>central</mirrorOf>
      <name>Aliyun Maven Mirror</name>
      <url>https://maven.aliyun.com/repository/central</url>
    </mirror>
  </mirrors>
</settings>
```

#### 3. Git配置

```bash
# 配置用户信息
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"

# 配置编辑器
git config --global core.editor "code --wait"  # VS Code
# git config --global core.editor "vim"       # Vim

# 配置换行符处理
git config --global core.autocrlf true  # Windows
git config --global core.autocrlf input  # macOS/Linux

# 验证配置
git config --list
```

#### 4. IDE配置

**IntelliJ IDEA配置**:

1. **插件安装**:
   - Lombok Plugin
   - Spring Boot Helper
   - SonarLint
   - GitToolBox
   - Rainbow Brackets

2. **代码模板**:
   - File Templates: 配置类、接口、枚举模板
   - Live Templates: 配置常用代码缩写
   - Postfix Templates: 配置后缀补全

3. **编码设置**:
   - File Encoding: UTF-8
   - Line Separator: LF (推荐)
   - Code Style: 导入项目代码规范文件

### 项目导入

#### 1. 克隆项目

```bash
# 克隆项目仓库
git clone https://github.com/your-org/Junmo Platform.git

# 进入项目目录
cd Junmo Platform

# 查看项目结构
ls -la
```

#### 2. 导入IDE

**IntelliJ IDEA**:
1. 打开IDEA，选择 "Open"
2. 选择项目根目录下的 `pom.xml`
3. 等待Maven依赖下载完成
4. 配置项目SDK为JDK 25

**Eclipse**:
1. File -> Import -> Existing Maven Projects
2. 选择项目根目录
3. 选择要导入的模块
4. Finish完成导入

#### 3. 依赖安装

```bash
# 安装项目依赖
mvn clean install

# 跳过测试安装
mvn clean install -DskipTests

# 强制更新依赖
mvn clean install -U
```

## 🏗️ 项目结构

### 目录结构

```
Junmo Platform/
├── pom.xml                           # 主POM文件
├── README.md                         # 项目说明
├── .gitignore                        # Git忽略文件
├── .gitattributes                    # Git属性文件
├── docs/                             # 项目文档
│   ├── DOCS_README.md               # 文档目录说明
│   ├── 01-规划设计层/                # 规划设计文档
│   ├── 02-系统设计层/                # 系统设计文档
│   ├── 03-实施支撑层/                # 实施支撑文档
│   └── 04-运维支持层/                # 运维支持文档
├── scripts/                          # 脚本文件
│   ├── build.sh                     # 构建脚本
│   ├── deploy.sh                    # 部署脚本
│   └── test.sh                      # 测试脚本
├── docker/                           # Docker相关文件
│   ├── Dockerfile                   # 应用镜像构建文件
│   ├── docker-compose.yml           # Docker Compose配置
│   └── docker-compose-dev.yml       # 开发环境配置
├── k8s/                             # Kubernetes配置文件
│   ├── namespace.yaml               # 命名空间
│   ├── configmap.yaml               # 配置映射
│   ├── secret.yaml                  # 密钥配置
│   ├── deployment.yaml              # 部署配置
│   ├── service.yaml                 # 服务配置
│   └── ingress.yaml                 # 入口配置
└── src/
    ├── main/
    │   ├── java/
    │   │   └── com/kev1n/Junmo Platform/
    │   │       ├── Junmo PlatformApplication.java    # 主启动类
    │   │       ├── config/                        # 配置类
    │   │       │   ├── SecurityConfig.java       # 安全配置
    │   │       │   ├── WebConfig.java            # Web配置
    │   │       │   ├── RedisConfig.java          # Redis配置
    │   │       │   ├── RabbitMQConfig.java       # RabbitMQ配置
    │   │       │   └── SwaggerConfig.java        # API文档配置
    │   │       ├── controller/                    # 控制器层
    │   │       │   ├── UserController.java       # 用户控制器
    │   │       │   ├── AuthController.java        # 认证控制器
    │   │       │   ├── FileController.java       # 文件控制器
    │   │       │   └── WorkflowController.java   # 工作流控制器
    │   │       ├── service/                       # 服务层
    │   │       │   ├── UserService.java          # 用户服务
    │   │       │   ├── AuthService.java          # 认证服务
    │   │       │   ├── FileService.java          # 文件服务
    │   │       │   └── WorkflowService.java      # 工作流服务
    │   │       ├── repository/                    # 数据访问层
    │   │       │   ├── UserRepository.java       # 用户仓库
    │   │       │   ├── RoleRepository.java       # 角色仓库
    │   │       │   └── FileRepository.java       # 文件仓库
    │   │       ├── entity/                        # 实体类
    │   │       │   ├── User.java                 # 用户实体
    │   │       │   ├── Role.java                 # 角色实体
    │   │       │   ├── Permission.java           # 权限实体
    │   │       │   └── File.java                 # 文件实体
    │   │       ├── dto/                           # 数据传输对象
    │   │       │   ├── UserDTO.java              # 用户DTO
    │   │       │   ├── CreateUserRequest.java    # 创建用户请求
    │   │       │   └── UpdateUserRequest.java    # 更新用户请求
    │   │       ├── mapper/                        # 对象映射器
    │   │       │   ├── UserMapper.java           # 用户映射器
    │   │       │   └── FileMapper.java           # 文件映射器
    │   │       ├── security/                      # 安全相关
    │   │       │   ├── JwtAuthenticationFilter.java # JWT过滤器
    │   │       │   ├── UserDetailsServiceImpl.java # 用户详情服务
    │   │       │   └── SecurityUtils.java        # 安全工具类
    │   │       ├── exception/                     # 异常处理
    │   │       │   ├── GlobalExceptionHandler.java # 全局异常处理器
    │   │       │   ├── BusinessException.java    # 业务异常
    │   │       │   └── ResourceNotFoundException.java # 资源未找到异常
    │   │       ├── util/                          # 工具类
    │   │       │   ├── DateUtils.java            # 日期工具
    │   │       │   ├── StringUtils.java          # 字符串工具
    │   │       │   ├── FileUtils.java            # 文件工具
    │   │       │   └── ValidationUtils.java      # 验证工具
    │   │       ├── constant/                      # 常量类
    │   │       │   ├── ApiConstants.java         # API常量
    │   │       │   ├── SecurityConstants.java    # 安全常量
    │   │       │   └── BusinessConstants.java    # 业务常量
    │   │       └── event/                         # 事件处理
    │   │           ├── UserCreatedEvent.java     # 用户创建事件
    │   │           ├── UserDeletedEvent.java     # 用户删除事件
    │   │           └── EventHandler.java         # 事件处理器
    │   └── resources/
    │       ├── application.yml                    # 主配置文件
    │       ├── application-dev.yml                # 开发环境配置
    │       ├── application-test.yml               # 测试环境配置
    │       ├── application-prod.yml               # 生产环境配置
    │       ├── db/
    │       │   └── migration/                     # 数据库迁移脚本
    │       │       ├── V1__Create_user_table.sql
    │       │       ├── V2__Create_role_table.sql
    │       │       └── V3__Create_permission_table.sql
    │       ├── static/                            # 静态资源
    │       │   ├── css/
    │       │   ├── js/
    │       │   └── images/
    │       ├── templates/                         # 模板文件
    │       │   ├── email/
    │       │   └── report/
    │       └── logback-spring.xml                 # 日志配置
    └── test/
        ├── java/
        │   └── com/kev1n/Junmo Platform/
        │       ├── Junmo PlatformApplicationTests.java # 主测试类
        │       ├── controller/                     # 控制器测试
        │       ├── service/                        # 服务测试
        │       ├── repository/                     # 仓库测试
        │       ├── integration/                    # 集成测试
        │       └── util/                          # 工具测试
        └── resources/
            ├── application-test.yml               # 测试配置
            └── test-data/                         # 测试数据
```

### 模块说明

#### 核心模块

1. **config**: 配置类模块
   - 安全配置、Web配置、数据库配置等
   - 使用@Configuration注解标记配置类

2. **controller**: 控制器层模块
   - 处理HTTP请求和响应
   - 使用@RestController或@Controller注解

3. **service**: 服务层模块
   - 业务逻辑处理
   - 使用@Service注解标记服务类

4. **repository**: 数据访问层模块
   - 数据库操作接口
   - 继承JpaRepository或使用@Query注解

5. **entity**: 实体类模块
   - 数据库表映射
   - 使用@Entity注解标记实体类

#### 支撑模块

1. **dto**: 数据传输对象
   - API请求和响应对象
   - 用于数据传输和验证

2. **mapper**: 对象映射器
   - 实体与DTO之间的转换
   - 使用MapStruct或手动映射

3. **security**: 安全模块
   - 认证和授权逻辑
   - JWT令牌处理等

4. **exception**: 异常处理模块
   - 自定义异常类
   - 全局异常处理器

5. **util**: 工具类模块
   - 通用工具方法
   - 避免重复代码

## 💻 编码规范

### 命名规范

#### 1. 包命名

**规则**: 全小写字母，单词之间用点号分隔

```java
// 正确示例
com.junmo.Junmo Platform.controller
com.junmo.Junmo Platform.service.impl
com.junmo.Junmo Platform.repository

// 错误示例
com.junmo.Junmo Platform.Controller
com.junmo.Junmo Platform.controller.user
```

#### 2. 类命名

**规则**: 大驼峰命名法（PascalCase）

```java
// 实体类
public class User {}
public class UserRole {}
public class OrderItem {}

// 服务类
public class UserService {}
public class OrderServiceImpl {}

// 控制器类
public class UserController {}
public class OrderManagementController {}

// 工具类
public class StringUtils {}
public class DateUtils {}

// 异常类
public class BusinessException {}
public class ResourceNotFoundException {}
```

#### 3. 方法命名

**规则**: 小驼峰命名法（camelCase），使用动词开头

```java
// 获取方法
public User getUserById(Long id) {}
public List<User> getAllUsers() {}
public boolean isUserActive(String username) {}

// 设置方法
public void setUserStatus(UserStatus status) {}
public void updateUserProfile(UserProfile profile) {}

// 业务方法
public User createUser(CreateUserRequest request) {}
public void deleteUser(Long userId) {}
public boolean validateUserInput(UserInput input) {}
```

#### 4. 变量命名

**规则**: 小驼峰命名法，见名知意

```java
// 成员变量
private String username;
private List<User> userList;
private LocalDateTime createdAt;

// 局部变量
String userEmail = "user@example.com";
int retryCount = 3;
boolean isValid = true;

// 常量
private static final String DEFAULT_PASSWORD = "123456";
public static final int MAX_RETRY_COUNT = 5;
```

### 代码风格

#### 1. 缩进和空格

**缩进**: 使用4个空格，不使用Tab

```java
public class UserController {
    
    private UserService userService;
    
    @GetMapping("/users/{id}")
    public ResponseEntity<UserDTO> getUser(@PathVariable Long id) {
        UserDTO user = userService.getUserById(id);
        return ResponseEntity.ok(user);
    }
}
```

**空格**: 操作符前后添加空格

```java
// 正确示例
int result = a + b * c;
if (condition1 && condition2) {
    // do something
}

// 错误示例
int result=a+b*c;
if(condition1&&condition2){
    // do something
}
```

#### 2. 大括号风格

**使用K&R风格**: 左大括号不换行

```java
// 正确示例
public void method() {
    if (condition) {
        // do something
    } else {
        // do other
    }
}

// 错误示例
public void method()
{
    if (condition)
    {
        // do something
    }
    else
    {
        // do other
    }
}
```

#### 3. 注释规范

**类注释**: 使用Javadoc格式

```java
/**
 * 用户控制器
 * 
 * 提供用户相关的API接口，包括用户查询、创建、更新、删除等功能
 * 
 * @author kev1n
 * @version 1.0.0
 * @since 2025-12-24
 */
@RestController
@RequestMapping("/api/users")
public class UserController {
    
}
```

**方法注释**: 说明方法功能、参数、返回值

```java
/**
 * 根据ID获取用户信息
 * 
 * @param id 用户ID，不能为空
 * @return 用户信息DTO
 * @throws ResourceNotFoundException 当用户不存在时抛出
 */
@GetMapping("/{id}")
public ResponseEntity<UserDTO> getUser(@PathVariable Long id) {
    UserDTO user = userService.getUserById(id);
    return ResponseEntity.ok(user);
}
```

**行内注释**: 解释复杂逻辑

```java
// 检查用户权限，只有管理员才能删除用户
if (!SecurityUtils.hasRole("ADMIN")) {
    throw new AccessDeniedException("只有管理员才能删除用户");
}

// 使用缓存提高查询性能
User cachedUser = cacheManager.get("user:" + userId);
if (cachedUser != null) {
    return cachedUser;
}
```

### 代码质量

#### 1. 异常处理

**异常体系结构**:

```
BaseException (基础异常)
├── BusinessException (业务异常) - HTTP 400
├── AuthException (认证异常基类)
│   ├── NotLoginException (未登录异常) - HTTP 401
│   │   ├── NOT_TOKEN - 未提供Token
│   │   ├── INVALID_TOKEN - Token无效
│   │   ├── TOKEN_TIMEOUT - Token已过期
│   │   ├── BE_REPLACED - 被顶下线
│   │   ├── KICK_OUT - 被踢下线
│   │   ├── TOKEN_FREEZE - Token被冻结
│   │   └── NO_PREFIX - Token前缀错误
│   ├── NotPermissionException (无权限异常) - HTTP 403
│   └── TokenInvalidException (Token无效异常) - HTTP 401
├── SystemException (系统异常) - HTTP 500
└── ValidationException (参数校验异常) - HTTP 400
```

**错误码枚举**:

```java
// 认证相关 40xxx
SUCCESS(200, "操作成功")
NOT_LOGIN(40100, "未登录")
INVALID_TOKEN(40102, "Token无效")
TOKEN_TIMEOUT(40103, "Token已过期")
NO_PERMISSION(40300, "无权限访问")

// 业务错误 42xxx
BUSINESS_ERROR(42000, "业务处理失败")
USER_NOT_FOUND(42001, "用户不存在")
USER_ALREADY_EXISTS(42002, "用户已存在")

// 系统错误 50xxx
SYSTEM_ERROR(50000, "系统错误")
DATABASE_ERROR(50001, "数据库错误")
```

**使用自定义异常**:

```java
// 业务异常
throw new BusinessException("用户名已存在: " + username);

// 认证异常
throw new NotLoginException(NotLoginException.TOKEN_TIMEOUT, "Token已过期");

// 权限异常
throw new NotPermissionException("user:delete", "无权限删除用户");

// 系统异常
throw new SystemException("数据库连接失败", e);

// 校验异常
throw new ValidationException("username", "用户名不能为空");
```

**全局异常处理**:

```java
@RestControllerAdvice
@Slf4j
public class GlobalExceptionHandler {
    
    // Sa-Token 认证异常
    @ExceptionHandler(NotLoginException.class)
    public ResponseEntity<ApiResponse<?>> handleNotLoginException(
            NotLoginException e, HttpServletRequest request) {
        String message = switch (e.getType()) {
            case NotLoginException.NOT_TOKEN -> "未提供Token";
            case NotLoginException.INVALID_TOKEN -> "Token无效";
            case NotLoginException.TOKEN_TIMEOUT -> "Token已过期，请重新登录";
            case NotLoginException.BE_REPLACED -> "账号已在其他设备登录";
            case NotLoginException.KICK_OUT -> "账号已被踢下线";
            default -> "未登录";
        };
        return ResponseEntity.status(HttpStatus.UNAUTHORIZED)
                .body(ApiResponse.error(401, message));
    }
    
    @ExceptionHandler(NotPermissionException.class)
    public ResponseEntity<ApiResponse<?>> handleNotPermissionException(
            NotPermissionException e, HttpServletRequest request) {
        return ResponseEntity.status(HttpStatus.FORBIDDEN)
                .body(ApiResponse.error(403, "无权限访问: " + e.getPermission()));
    }
    
    // 业务异常
    @ExceptionHandler(BusinessException.class)
    public ResponseEntity<ApiResponse<?>> handleBusinessException(
            BusinessException e, HttpServletRequest request) {
        return ResponseEntity.status(e.getHttpStatus())
                .body(ApiResponse.error(e.getHttpStatus(), e.getMessage()));
    }
    
    // 系统异常
    @ExceptionHandler(SystemException.class)
    public ResponseEntity<ApiResponse<?>> handleSystemException(
            SystemException e, HttpServletRequest request) {
        log.error("系统异常: {}", e.getMessage(), e);
        return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR)
                .body(ApiResponse.error(500, "系统错误，请稍后重试"));
    }
    
    // 参数校验异常
    @ExceptionHandler(MethodArgumentNotValidException.class)
    public ResponseEntity<ApiResponse<?>> handleMethodArgumentNotValidException(
            MethodArgumentNotValidException e, HttpServletRequest request) {
        List<String> errors = e.getBindingResult().getFieldErrors()
                .stream()
                .map(error -> error.getField() + ": " + error.getDefaultMessage())
                .collect(Collectors.toList());
        return ResponseEntity.status(HttpStatus.BAD_REQUEST)
                .body(ApiResponse.error(400, "参数校验失败", errors));
    }
    
    // 通用异常
    @ExceptionHandler(Exception.class)
    public ResponseEntity<ApiResponse<?>> handleException(
            Exception e, HttpServletRequest request) {
        log.error("未捕获的异常: {}", e.getMessage(), e);
        return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR)
                .body(ApiResponse.error(500, "系统错误，请稍后重试"));
    }
}
```

#### 2. 参数验证

**使用Bean Validation**:

```java
public class CreateUserRequest {
    
    @NotBlank(message = "用户名不能为空")
    @Size(min = 3, max = 50, message = "用户名长度必须在3-50个字符之间")
    private String username;
    
    @NotBlank(message = "邮箱不能为空")
    @Email(message = "邮箱格式不正确")
    private String email;
    
    @NotBlank(message = "密码不能为空")
    @Size(min = 8, message = "密码长度不能少于8位")
    @Pattern(regexp = "^(?=.*[a-z])(?=.*[A-Z])(?=.*\\d)(?=.*[@$!%*?&])[A-Za-z\\d@$!%*?&]",
             message = "密码必须包含大小写字母、数字和特殊字符")
    private String password;
}
```

**控制器验证**:

```java
@PostMapping
public ResponseEntity<UserDTO> createUser(@Valid @RequestBody CreateUserRequest request) {
    UserDTO user = userService.createUser(request);
    return ResponseEntity.status(HttpStatus.CREATED).body(user);
}
```

#### 3. 日志规范

**使用SLF4J + Logback**:

```java
@Slf4j
@Service
public class UserServiceImpl implements UserService {
    
    public User createUser(CreateUserRequest request) {
        log.info("开始创建用户: {}", request.getUsername());
        
        try {
            User user = buildUser(request);
            User savedUser = userRepository.save(user);
            
            log.info("用户创建成功: id={}, username={}", savedUser.getId(), savedUser.getUsername());
            return savedUser;
            
        } catch (Exception e) {
            log.error("用户创建失败: username={}, error={}", request.getUsername(), e.getMessage(), e);
            throw new BusinessException("用户创建失败: " + e.getMessage());
        }
    }
}
```

**日志级别使用**:
- **ERROR**: 系统错误，需要立即关注
- **WARN**: 警告信息，可能存在问题
- **INFO**: 重要业务流程信息
- **DEBUG**: 调试信息，仅在开发环境使用

## 🔧 开发流程

### Git工作流

#### 1. 分支策略

**主要分支**:
- **main**: 主分支，用于生产环境
- **develop**: 开发分支，用于集成测试
- **release**: 发布分支，用于发布准备
- **hotfix**: 热修复分支，用于紧急修复

**功能分支**:
- **feature/user-management**: 用户管理功能
- **feature/file-upload**: 文件上传功能
- **feature/workflow-engine**: 工作流引擎

#### 2. 分支操作

**创建功能分支**:
```bash
# 切换到develop分支
git checkout develop

# 拉取最新代码
git pull origin develop

# 创建功能分支
git checkout -b feature/user-management
```

**提交代码**:
```bash
# 查看修改状态
git status

# 添加修改文件
git add .

# 提交代码
git commit -m "feat: 添加用户管理功能

- 实现用户CRUD操作
- 添加参数验证
- 完善单元测试

Closes #123"
```

**推送分支**:
```bash
# 推送到远程仓库
git push origin feature/user-management

# 创建Pull Request
# 在GitHub/GitLab上创建PR到develop分支
```

**合并分支**:
```bash
# 切换到develop分支
git checkout develop

# 合并功能分支
git merge feature/user-management

# 推送develop分支
git push origin develop

# 删除功能分支
git branch -d feature/user-management
git push origin --delete feature/user-management
```

#### 3. 提交规范

**提交消息格式**:
```
<type>(<scope>): <subject>

<body>

<footer>
```

**类型说明**:
- **feat**: 新功能
- **fix**: 修复bug
- **docs**: 文档更新
- **style**: 代码格式调整
- **refactor**: 代码重构
- **test**: 测试相关
- **chore**: 构建过程或辅助工具的变动

**示例**:
```
feat(auth): 添加JWT认证功能

- 实现JWT令牌生成和验证
- 添加认证过滤器
- 完善认证相关测试

Closes #45
```

### 代码审查

#### 1. Pull Request流程

**创建PR**:
1. 在GitHub/GitLab上创建Pull Request
2. 选择目标分支（通常是develop）
3. 填写PR描述
4. 添加相关标签和审查者

**PR模板**:
```markdown
## 变更描述
简要描述本次变更的内容和目的

## 变更类型
- [ ] 新功能
- [ ] Bug修复
- [ ] 文档更新
- [ ] 性能优化
- [ ] 代码重构

## 测试
- [ ] 单元测试通过
- [ ] 集成测试通过
- [ ] 手动测试完成

## 检查清单
- [ ] 代码符合项目规范
- [ ] 添加了必要的测试
- [ ] 更新了相关文档
- [ ] 没有遗留的TODO或FIXME

## 相关Issue
Closes #123

## 截图（如适用）
添加相关截图或GIF

## 其他说明
其他需要说明的内容
```

#### 2. 审查要点

**代码质量**:
- 代码是否符合编码规范
- 逻辑是否清晰易懂
- 是否存在潜在的bug
- 性能是否有问题

**架构设计**:
- 设计是否合理
- 是否遵循SOLID原则
- 是否考虑扩展性
- 是否与现有架构一致

**测试覆盖**:
- 是否有足够的单元测试
- 测试用例是否覆盖主要场景
- 测试代码质量是否合格

**安全性**:
- 是否有安全漏洞
- 输入验证是否充分
- 权限控制是否正确

### 测试策略

#### 1. 单元测试

**测试框架**: JUnit 5 + Mockito + AssertJ

**测试示例**:
```java
@ExtendWith(MockitoExtension.class)
class UserServiceTest {
    
    @Mock
    private UserRepository userRepository;
    
    @Mock
    private PasswordEncoder passwordEncoder;
    
    @InjectMocks
    private UserServiceImpl userService;
    
    @Test
    @DisplayName("应该成功创建用户")
    void shouldCreateUserSuccessfully() {
        // Given
        CreateUserRequest request = CreateUserRequest.builder()
            .username("testuser")
            .email("test@example.com")
            .password("password123")
            .build();
        
        User savedUser = User.builder()
            .id(1L)
            .username("testuser")
            .email("test@example.com")
            .build();
        
        when(userRepository.existsByUsername("testuser")).thenReturn(false);
        when(userRepository.existsByEmail("test@example.com")).thenReturn(false);
        when(passwordEncoder.encode("password123")).thenReturn("encodedPassword");
        when(userRepository.save(any(User.class))).thenReturn(savedUser);
        
        // When
        UserDTO result = userService.createUser(request);
        
        // Then
        assertThat(result).isNotNull();
        assertThat(result.getUsername()).isEqualTo("testuser");
        assertThat(result.getEmail()).isEqualTo("test@example.com");
        
        verify(userRepository).save(any(User.class));
    }
}
```

#### 2. 集成测试

**测试框架**: Spring Boot Test + Testcontainers

**测试示例**:
```java
@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
@Testcontainers
class UserControllerIntegrationTest {
    
    @Container
    static MySQLContainer<?> mysql = new MySQLContainer<>("mysql:8.0")
            .withDatabaseName("testdb")
            .withUsername("test")
            .withPassword("test");
    
    @Autowired
    private TestRestTemplate restTemplate;
    
    @DynamicPropertySource
    static void configureProperties(DynamicPropertyRegistry registry) {
        registry.add("spring.datasource.url", mysql::getJdbcUrl);
        registry.add("spring.datasource.username", mysql::getUsername);
        registry.add("spring.datasource.password", mysql::getPassword);
    }
    
    @Test
    @DisplayName("应该完成用户注册流程")
    void shouldCompleteUserRegistrationFlow() {
        // Given
        CreateUserRequest request = CreateUserRequest.builder()
            .username("newuser")
            .email("newuser@example.com")
            .password("password123")
            .build();
        
        // When
        ResponseEntity<UserDTO> response = restTemplate.postForEntity(
            "/api/users", request, UserDTO.class);
        
        // Then
        assertThat(response.getStatusCode()).isEqualTo(HttpStatus.CREATED);
        assertThat(response.getBody()).isNotNull();
        assertThat(response.getBody().getUsername()).isEqualTo("newuser");
    }
}
```

#### 3. 测试覆盖率

**配置Jacoco**:
```xml
<plugin>
    <groupId>org.jacoco</groupId>
    <artifactId>jacoco-maven-plugin</artifactId>
    <version>0.8.8</version>
    <executions>
        <execution>
            <goals>
                <goal>prepare-agent</goal>
            </goals>
        </execution>
        <execution>
            <id>report</id>
            <phase>test</phase>
            <goals>
                <goal>report</goal>
            </goals>
        </execution>
        <execution>
            <id>check</id>
            <goals>
                <goal>check</goal>
            </goals>
            <configuration>
                <rules>
                    <rule>
                        <element>BUNDLE</element>
                        <limits>
                            <limit>
                                <counter>INSTRUCTION</counter>
                                <value>COVEREDRATIO</value>
                                <minimum>0.80</minimum>
                            </limit>
                        </limits>
                    </rule>
                </rules>
            </configuration>
        </execution>
    </executions>
</plugin>
```

## 🚀 构建与部署

### 本地构建

#### 1. 编译项目

```bash
# 清理并编译
mvn clean compile

# 编译并运行测试
mvn clean test

# 编译并打包
mvn clean package

# 跳过测试打包
mvn clean package -DskipTests
```

#### 2. 运行应用

```bash
# 使用Maven运行
mvn spring-boot:run

# 使用Java运行jar包
java -jar target/Junmo Platform-1.0.0.jar

# 指定配置文件运行
java -jar target/Junmo Platform-1.0.0.jar --spring.profiles.active=dev

# 指定JVM参数运行
java -Xms512m -Xmx1024m -jar target/Junmo Platform-1.0.0.jar
```

#### 3. 开发环境启动

```bash
# 启动依赖服务
docker-compose -f docker-compose-dev.yml up -d

# 启动应用
mvn spring-boot:run -Dspring-boot.run.profiles=dev

# 查看日志
tail -f logs/Junmo Platform.log
```

### Docker构建

#### 1. Dockerfile

```dockerfile
# 多阶段构建
FROM maven:3.9-openjdk-25 AS builder

WORKDIR /app
COPY pom.xml .
COPY src ./src

# 构建应用
RUN mvn clean package -DskipTests

# 运行时镜像
FROM openjdk:25-jre-slim

WORKDIR /app

# 创建应用用户
RUN addgroup --system spring && adduser --system spring --ingroup spring

# 复制jar包
COPY --from=builder /app/target/Junmo Platform-*.jar app.jar

# 设置权限
RUN chown spring:spring app.jar
USER spring

# 健康检查
HEALTHCHECK --interval=30s --timeout=3s --start-period=60s --retries=3 \
  CMD curl -f http://localhost:8080/actuator/health || exit 1

# 暴露端口
EXPOSE 8080

# 启动应用
ENTRYPOINT ["java", "-jar", "app.jar"]
```

#### 2. 构建镜像

```bash
# 构建镜像
docker build -t Junmo Platform:1.0.0 .

# 构建镜像并指定标签
docker build -t Junmo Platform:latest -t Junmo Platform:1.0.0 .

# 查看镜像
docker images | grep Junmo Platform
```

#### 3. 运行容器

```bash
# 运行容器
docker run -d \
  --name Junmo Platform \
  -p 8080:8080 \
  -e SPRING_PROFILES_ACTIVE=prod \
  Junmo Platform:1.0.0

# 查看容器日志
docker logs -f Junmo Platform

# 进入容器
docker exec -it Junmo Platform bash
```

### CI/CD流程

#### 1. GitHub Actions配置

```yaml
# .github/workflows/ci-cd.yml
name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  test:
    runs-on: ubuntu-latest
    
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: password
          MYSQL_DATABASE: testdb
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3

    steps:
    - uses: actions/checkout@v3
    
    - name: Set up JDK 25
      uses: actions/setup-java@v3
      with:
        java-version: '25'
        distribution: 'temurin'
    
    - name: Cache Maven dependencies
      uses: actions/cache@v3
      with:
        path: ~/.m2
        key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
        restore-keys: ${{ runner.os }}-m2
    
    - name: Run tests
      run: mvn clean test
      env:
        SPRING_DATASOURCE_URL: jdbc:mysql://localhost:3306/testdb
        SPRING_DATASOURCE_USERNAME: root
        SPRING_DATASOURCE_PASSWORD: password
    
    - name: Generate test report
      uses: dorny/test-reporter@v1
      if: success() || failure()
      with:
        name: Maven Tests
        path: target/surefire-reports/*.xml
        reporter: java-junit
    
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        file: ./target/site/jacoco/jacoco.xml

  build:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop'
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Log in to Container Registry
      uses: docker/login-action@v2
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v4
      with:
        images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
    
    - name: Build and push Docker image
      uses: docker/build-push-action@v4
      with:
        context: .
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}

  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    environment: production
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Deploy to production
      run: |
        echo "Deploying to production..."
        # 部署脚本
```

## 🔍 调试技巧

### 本地调试

#### 1. IDEA远程调试

**配置JVM参数**:
```bash
java -agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=5005 -jar app.jar
```

**IDEA调试配置**:
1. Run -> Edit Configurations
2. 点击+号，选择Remote JVM Debug
3. 配置Host和Port（localhost:5005）
4. 设置断点，点击Debug按钮

#### 2. 日志调试

**配置日志级别**:
```yaml
# application-dev.yml
logging:
  level:
    com.junmo.Junmo Platform: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
```

**使用日志输出调试信息**:
```java
@Slf4j
@Service
public class UserService {
    
    public User createUser(CreateUserRequest request) {
        log.debug("开始创建用户，请求参数: {}", request);
        
        try {
            User user = buildUser(request);
            log.debug("用户对象构建完成: {}", user);
            
            User savedUser = userRepository.save(user);
            log.debug("用户保存成功，ID: {}", savedUser.getId());
            
            return savedUser;
        } catch (Exception e) {
            log.error("用户创建失败", e);
            throw e;
        }
    }
}
```

### 性能调试

#### 1. JVM性能分析

**启用JMX监控**:
```bash
java -Dcom.sun.management.jmxremote \
     -Dcom.sun.management.jmxremote.port=9999 \
     -Dcom.sun.management.jmxremote.authenticate=false \
     -Dcom.sun.management.jmxremote.ssl=false \
     -jar app.jar
```

**使用JVisualVM**:
1. 启动JVisualVM
2. 添加远程连接
3. 监控CPU、内存、线程使用情况
4. 分析内存堆转储

#### 2. 数据库性能调试

**启用SQL日志**:
```yaml
spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
```

**分析慢查询**:
```sql
-- 启用慢查询日志
SET GLOBAL slow_query_log = 'ON';
SET GLOBAL long_query_time = 1;

-- 查看慢查询
SELECT * FROM mysql.slow_log ORDER BY start_time DESC LIMIT 10;
```

### 网络调试

#### 1. HTTP请求调试

**使用curl测试API**:
```bash
# GET请求
curl -X GET "http://localhost:8080/api/users/1" \
     -H "Authorization: Bearer YOUR_TOKEN"

# POST请求
curl -X POST "http://localhost:8080/api/users" \
     -H "Content-Type: application/json" \
     -H "Authorization: Bearer YOUR_TOKEN" \
     -d '{"username":"testuser","email":"test@example.com"}'

# 查看响应头
curl -I "http://localhost:8080/api/users/1"
```

#### 2. 网络抓包

**使用Wireshark**:
1. 启动Wireshark
2. 选择网络接口
3. 设置过滤器（如tcp.port == 8080）
4. 分析HTTP请求和响应

**使用tcpdump**:
```bash
# 抓取8080端口的流量
tcpdump -i any -n -s 0 -w capture.pcap 'tcp port 8080'

# 分析抓包文件
tcpdump -r capture.pcap -A
```

## 📚 学习资源

### 官方文档

- [Spring Boot官方文档](https://docs.spring.io/spring-boot/docs/current/reference/htmlsingle/)
- [Spring Framework文档](https://docs.spring.io/spring-framework/docs/current/reference/html/)
- [Maven官方文档](https://maven.apache.org/guides/)
- [Git官方文档](https://git-scm.com/doc)

### 推荐书籍

- 《Spring Boot实战》
- 《Spring in Action》
- 《Effective Java》
- 《Clean Code》
- 《设计模式》

### 在线课程

- Spring Boot官方教程
- Baeldung Spring教程
- Oracle Java教程
- Git官方教程

### 技术博客

- Spring官方博客
- Baeldung
- DZone
- InfoQ

---

*本开发指南将随着技术发展和项目需求持续更新，为开发团队提供最新的开发指导。*