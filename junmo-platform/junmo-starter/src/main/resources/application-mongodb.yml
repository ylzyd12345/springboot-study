# MongoDB 配置
spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/junmo_platform}
      database: ${MONGODB_DATABASE:junmo_platform}
      auto-index-creation: true
      auto-time-logs: true